'use client';

// Compatibility export for legacy imports. The Dean now uses the shared
// actionable signatory queue rather than a read-only oversight dashboard.
export { default } from '@/components/signatory/SignatoryDashboard';
